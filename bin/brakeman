#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Only report warnings with Medium or High confidence (ignore Weak warnings)
ARGV.unshift("-w3")

load Gem.bin_path("brakeman", "brakeman")
